<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <title>CRUD App</title>
  <style>
    .menu {
      margin-bottom: 32px;
      padding: 0px 32px;
      background-color: #007bff;
    }

    .menu a {
      color: #ffffff;
      padding: 5px 10px;
      transition: all .3s ease-in-out;
      height: 100%;
      display: inline-block;
    }

    .menu a:hover {
      text-decoration: none;
      background-color: #024892;
    }

    .menu a.active {
      pointer-events: none;
      opacity: 0.5;
    }

    .jumbotron {
      margin-bottom: 0px;
    }

    .container {
      margin-bottom: 72px;
    }

    .container--flex {
      display: flex;
      flex-wrap: wrap;
    }

    .container--flex--space {
      justify-content: space-around;
    }

    .person {
      border-radius: 10px;
      background-color: #007bff;
      -webkit-box-shadow: 0 10px 6px -6px #777;
      -moz-box-shadow: 0 10px 6px -6px #777;
      box-shadow: 0 10px 6px -6px #777;
      color: #ffffff;
      padding: 25px 15px;
      margin: 0px 25px 25px 25px;
      min-width: 300px;
    }

    .person p {
      margin: 0;
    }

    .person ul {
      list-style: none;
      margin-top: 10px;
      padding-left: 0;
    }

    form {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      min-width: 468px;
      max-width: 468px;
      padding: 25px;
      border-radius: 5px;
      margin-top: 25px;
      background-color: #f0f0f0;
    }

    form .input {
      display: flex;
      margin-bottom: 5px;
    }

    .input input {
      border: 1px solid #d1d1d1;
      border-radius: 5px;
      width: 70%;
      outline: none;
      padding: 2px 10px;
    }

    .input label {
      margin: 0;
      width: 30%;
    }

    footer {
      position: fixed;
      left: 0;
      bottom: 0;
      width: 100%;
      text-align: center;
    }

    #socials {
      background-color: #007bff;
      padding: 10px;
    }

    #socials svg {
      fill: #ffffff;
      transition: all .3s ease-in-out;
      opacity: 0.8;
    }

    #socials a:hover svg {
      opacity: 1;
    }

    #copy {
      font-size: 11px;
      background-color: #e9ecef;
      padding: 5px;
    }

    #status-message {
      margin-bottom: 25px;
    }

    #status-message div {
      background-color: #299100;
      color: #ffffff;
      display: inline-block;
      border-radius: 5px;
      padding: 10px;
      margin-left: 25px;
    }
  </style>
</head>
<body>
  
  <div class="jumbotron">
    <h1><%= title %></h1>
    <p><%= subtitle %></p>
  </div>

  <div class="menu">
    <a href="/" class="<% if (firstMenuItem) { %> <%= 'active' %> <% } %>">ACTIONS</a>
    <a href="/read" class="<% if (secondMenuItem) { %> <%= 'active' %> <% } %>">OBJECTS</a>
  </div>